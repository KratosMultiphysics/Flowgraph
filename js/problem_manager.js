let problem_files = {}      // List of files loaded in the workspace
let problem_modelparts = {} // List of modelparts loaded in the workspace
let model_nodes = {}        // List of 'Models' nodes in the workspace

problem_files["materials"] = {}

LGraphCanvas.link_type_colors["string"] = "#F0F"